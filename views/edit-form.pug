doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    title Express Gallery || Edit The Photo
    link(href='/css/styles.css', rel='stylesheet')
  body
    div.container
      div(class=['header', 'clearfix'])
        div.logo
          a(href='/')
            img(src='/images/logo.jpg')
        div.summary
          p Welcome to our online portfolio, we are small group of passionate designers and architects, looking to change and create amazing digital images to inspire others to follow in our innovative steps. Free Architect WordPress Theme
      div.main-content
        div.form-container
          h1 Edit The Photo
          form(action="/gallery/" + selectedPhoto.id, method="post", enctype="application/x-www-form-urlencoded")
            input(type="hidden", name="_method", value="PUT")
            p.form-link
              label(for="link") Link
              br
              input(type="text", name="link", value= selectedPhoto.link)
            p.form-author
              label(for="author") Author
              br
              input(type="text", name="author", value= selectedPhoto.author)
            p.form-description
              label(for="description") Description
              br
              textarea(name="description")
                | #{selectedPhoto.description}
            p.form-submit
              input(type="submit", value="Submit Changes")